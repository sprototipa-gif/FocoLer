import { DifficultyLevel } from "../types";

// Service cleared to remove API dependencies for Client-Side Only version.
// This ensures no keys are needed and the app runs fully in the browser using Web APIs.

export const generateStory = async (level: DifficultyLevel): Promise<string> => {
  return "Funcionalidade desabilitada nesta versão.";
};